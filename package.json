{
  "name": "taskpulse-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && ./node_modules/.bin/tsc",
    "start": "node backend/dist/server.js"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
